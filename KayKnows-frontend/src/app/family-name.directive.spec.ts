import { FamilyNameDirective } from './family-name.directive';

describe('FamilyNameDirective', () => {
  it('should create an instance', () => {
    const directive = new FamilyNameDirective();
    expect(directive).toBeTruthy();
  });
});
